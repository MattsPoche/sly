;; (if #f
;; 	(begin
;; 	  (define (foo x)
;; 		(if (> x 0)
;; 			(begin (display x)
;; 				   (foo (- x 1)))
;; 			(void)))
;; 	  (foo 10))
;; 	(void))

(define x 3)
(define (foo x y) (* x y))
(display (/ (foo 69 420) x))
(display x)
