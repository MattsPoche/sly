(require "sly-lib/base.sly")

(let ([x 69] [y 420])
 (displayln x)
 (displayln y)
 (displayln (+ x y)))

(let loop ([i 0])
  (when (< i 10)
	(displayln i)
	(loop (+ i 1))))

(let* ([x 69]
	   [y 420]
	   [z (* x y)])
  (displayln x)
  (displayln y)
  (displayln z))
